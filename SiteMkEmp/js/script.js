// Comando para chamar o slide show de forma simples
$('.slider-principal').slick({
    dots: true,
    infinite: true,
    speed: 200,
    slidesToShow: 1,
    adaptiveHeight: true,
    fade: true,
    autoplay: true
});